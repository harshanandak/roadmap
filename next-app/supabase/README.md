## Legacy Supabase Directory

This directory is legacy and read-only.

Rules:
- Do not add new migrations here.
- Do not treat this directory as the source of truth for resets, pushes, or schema review.
- Port any still-needed changes into the canonical root directory at `/supabase` deliberately.

The active Supabase project root for this repository is the root-level `supabase/` directory.
